import {Product1}from '../../../database.js';

const createProduct = async(name, description, images, category, extras, size, characteristics, released, price, stock)=>{
    
};

export default createProduct;