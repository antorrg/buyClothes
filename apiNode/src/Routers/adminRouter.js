import {Router} from 'express';
// import {userLogin, userCreate} from '../UsersHandlers/userLoginHandler'
// import { validUserCreate, validUserLog } from '../utils/validateUsers';

const adminRouter = Router();

// adminRouter.post('/user/login', validUserLog, userLogin)
// adminRouter.post('/admin/create', validUserCreate, userCreate)

export default adminRouter;